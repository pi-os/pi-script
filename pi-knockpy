#!/bin/bash
loop=true;
figlet Pi - knockpy;
echo "";
while $loop
 do
  read -e -p "pi-knockpy > " arg;
  history -s "$arg";
  if [[ $arg = "q" || $arg = "e" || $arg = "exit" || $arg = "quit" ]]
  then
   loop=false;
  elif [[ $arg = "help" || $arg = "h" || $arg = "?" ]]
  then
   knockpy -h;
  elif [[ $arg = "man" || $arg = "manual" ]]
  then
   man knockpy;
  elif [[ ${arg%% *} = "cd" ]]
  then
   $arg;
  elif [[ $arg = "" ]]
  then
   echo -n; 
  elif which "${arg%% *}"
  then
   echo;
   echo "result:";
   $arg
  else
   sudo knockpy $arg;
  fi	
 done
